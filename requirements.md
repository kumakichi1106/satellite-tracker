# 要件

## ゴール

TLEデータをもとに衛星の現在位置・予測軌道・地上局からの可視時間帯を計算し、3D地球上に表示するWebアプリケーションを作成する。
衛星データ処理、軌道計算、地上局からの可視判定、3D可視化をReact / TypeScriptで実装する。

## 現在のスコープ

- CelesTrak形式のTLEデータを扱う
- satellite.jsを使ってTLEから衛星の現在位置を計算する
- 衛星の緯度、経度、高度を算出する
- 算出した緯度経度を3D地球上の座標へ変換する
- React Three Fiberで地球を3D表示する
- 日本付近を見やすい初期ビューとして表示する
- 衛星の現在位置を地球上にマーカー表示する
- 選択した衛星の予測軌道を表示する
- 衛星名、緯度、経度、高度を表示する
- 地上局から見た選択中衛星の可視状態を表示する
  - 仰角
  - 方位角
  - 距離
  - AOS
  - LOS
  - 最大仰角
  - 可視時間帯
- TLEグループを切り替えられるようにする
  - Space Stations
  - Weather
  - GPS Operational
  - Active Satellites
- 衛星名で検索できるようにする
- 表示件数を制限し、衛星数が多い場合でも描画負荷を抑える
- CelesTrakへの過剰アクセスを避けるため、開発中はモックTLEデータを利用できるようにする

## 非機能要件

- GitHub Pagesで公開できるSPAとして実装する
- React / TypeScript / Viteを使用する
- 3D描画にはReact Three Fiberを使用する
- 衛星位置計算や可視判定などのロジックはUIから分離する
- API通信、状態管理、表示コンポーネントの責務を分ける
- 大量衛星データを扱う場合でも、不要な位置計算と描画を抑制する
- CelesTrakのアクセス制限を考慮し、不必要な再取得を避ける